RummiRogue details

basic rules for default rummikub
  -106 tiles, 1->13, 2 jokers, Y/R/BLK/BLU
  -group (3 colors)
  -run (consecutive 3)
  
start by taking 14 tiles
can only place on table if able to make 30 pts (can not use tiles already played on the table)
cannot lay tile just drawn, must wait till next turn

joker can be taken from the table and replaced by a valid tile, but must be played not 'kept'
joker has any value or any color

-1 min time limit if exceeded draw 3 tiles

winning can achieved via round limit or clearing your rack

if rummi achieved players add up value on rack as negative values (joker on rack is 30)
if pool is emptied before rummi, player with lowest rack wins round
1->13 x 4 (colors) x 2 (double each color) + 2 jokers = 103

the winner gets the positive amount of tiles remaining on each players rack
ie player 1 (W)  + 12, player 2 (L) -5, player 3 (L) -7

--[NOTES ON ISSUES]--
--(X) When splitting runs it still shows valid if you take from the middle [split the sets]

--(X) Sets move around the table when added to, gross// somewhat mitigated

--(X) aspect ratios gets messed up update window management
  --() reset to previous window width and height after F11

--(X) adding a tile to a set where the tile is already in the spot for it to go won't allow

--(X) adding two jokers and a tile is still invalid!

--(X) adding bridge to two tiles broken. 2, 3 , bridge 8 doesn't work

--() adding and removing tile increases the score of the set, remove

--()


--[GOALS]--

(X) Test out creating a grid for the board
(1/2) Add turn limit 
    -round 1 set to 25 moves, go down by 2 every round
(1/2) Add score minimum

--TILES 
  -(X)  bridge -completes a run between two numbers 
  -(X) joker --implemented (mostly still some bugs but pretty solid)

--MULTIPLIERS
  -() Xs for runs
  -() Xs for groups
  -() Set size

--Add 'perks'
  -

--Consumables
  -Discard

--[TO-HAVES]--
-loading game screen when init game starts
-background music
-continue game
-challenges maybe? -- IN PROGRESS
-text blitting animation (blitting may be the wrong word but text loading char by char)
-animation adding the tiles to the rack - IN PROGRESS
--have child and parents set properly!!! that will fix all issues!!!!


-hover 'bobble' animation for tiles
-(X) drop shadow for buttons that goes away on press down
-counting animation after every draw button click that adds to table total
-maybe do +tile number animation where the 'bobble' occurs for every tile on table, maybe only do per set

--really need to look at this for desing-- THE MCGUFFIN
-finish completion (clear tiles after 35 draws is the current default one)
-after finishing this you get a reward you can use to buy the widgets
-pick the next completion option!

-bomb tile -- removes a set from the table
-locked tile (only run or group)
-multiplier tile

--passive
-+(number) for every run with (number) tiles
-tile removal at the shop

